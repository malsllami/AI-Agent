

اعضاء الجديد# ملف البدء الكامل — مدير المشاريع الذكي (محمد)

> هذا الملف يُعطى لـ Claude Code داخل VS Code كنقطة انطلاق كاملة. يحتوي على القواعد الثابتة لكل مشاريع محمد + كل القرارات التي تم الاتفاق عليها بخصوص هذا المشروع تحديدًا في محادثة سابقة. لا حاجة لإعادة شرح أي شيء من البداية — الالتزام بما هو مكتوب هنا إلزامي، وأي نقطة غير موجودة هنا تُسأل لمحمد مباشرة قبل التنفيذ.

---

## القسم الأول: القواعد الثابتة لكل المشاريع (لا تُخالف أبدًا)

### اللغة
- كل تواصل مع محمد باللغة العربية فقط.
- عند عرض أي مقترح أو خطة عمل على محمد، تُكتب **بالعربي البسيط بدون أي رموز برمجية أو مصطلحات تقنية أو علمية** — يفهمها أي شخص غير متخصص.
- التعليقات داخل الكود نفسه (comments) تُكتب بالعربي والإنجليزي معًا لكل قسم أو دالة رئيسية.

### البنية التقنية الإلزامية
- قاعدة البيانات: **Google Sheets** فقط.
- الاستضافة: **GitHub Pages**.
- طبقة الربط: **Google Apps Script** فقط — لا اتصال مباشر آخر بأي قاعدة بيانات.

### تسمية الجداول والأعمدة (إلزامي ١٠٠٪)
- كل أسماء الجداول والأعمدة باللغة العربية فقط.
- ممنوع: الإنجليزية، الشرطة `-`، الشرطة السفلية `_`، أي رمز خاص، أكثر من مسافة واحدة بين الكلمات.
- مثال صحيح: `اسم الموظف` — مثال خاطئ: `employee_name` أو `اسم-الموظف`.

### إنشاء الجداول
- كل الجداول والأعمدة والتنسيقات تُنشأ تلقائيًا عبر كود Google Apps Script — لا إنشاء يدوي أبدًا.
- عند الإضافة مستقبلًا: دالة جديدة مستقلة، بدون حذف أو إعادة إنشاء أو تعديل بيانات قديمة (إضافة فقط).

### عند وجود أي غموض
- **لا تخمين، لا افتراض.** تُطرح أسئلة توضيحية وينتظر Claude Code إجابة واضحة قبل أي تنفيذ فعلي.

### جودة البرمجة
- أحدث الإصدارات المستقرة، أفضل الممارسات، Clean Code، مبادئ SOLID عند الحاجة.
- إزالة أي كود غير مستخدم. ممنوع مكتبات قديمة أو مهجورة.

### تنظيم الملفات
- عند كبر المشروع: تقسيم حسب المسؤولية (`components/`, `pages/`, `services/`, `utils/`, `styles/`, `config/`)، كل ملف وظيفة واحدة، مع ترميز عربي/إنجليزي أعلى كل ملف.
- ممنوع وضع المشروع بالكامل في ملف واحد.

### مصدر البيانات
- كل البيانات (نصوص، قوائم، إعدادات) من Google Sheets فقط — ممنوع أي بيانات ثابتة داخل الكود.

### معايير الواجهة العامة
- تصميم احترافي عصري بسيط أنيق سريع، متجاوب مع كل الشاشات، بلا ألوان نمطية للذكاء الاصطناعي وبلا ظلال مبالغ فيها.
- الجداول: الصف الأول ثابت (sticky) وملوّن، النص محاذي للوسط، حجم الخط ١٢ وعريض.
- حقول الإدخال تقبل الأرقام العربية والإنجليزية معًا.
- حقول التاريخ تعرض الميلادي والهجري معًا.
- كل البطاقات وصناديق الإدخال بحواف دائرية.

### سلامة البيانات (حرج جدًا)
- ممنوع نهائيًا: حذف بيانات مستخدمين، حذف جداول موجودة، حذف أعمدة موجودة.
- أي تحديث = إضافة فقط أو تعديل آمن مع توافق كامل مع الإصدارات السابقة.

### الترتيب الإلزامي قبل أي مهمة
1. مراجعة هذا الملف بالكامل.
2. التأكد من عدم مخالفة أي قاعدة.
3. سؤال محمد عند أي غموض.
4. اقتراح أفضل الممارسات عند الحاجة (بدون فرضها).
5. عرض المقترح وخطة العمل بعربي بسيط وانتظار موافقة محمد **قبل** أي تعديل فعلي على الكود أو المشاريع الحالية.
6. التنفيذ فقط بعد الموافقة الصريحة.

---

## القسم الثاني: سياق ومهمة "مدير المشاريع الذكي" (هذا المشروع تحديدًا)

### الهدف العام
نظام يقوم بـ:
1. قراءة مشاريع محمد الحالية (موجودة على GitHub + نسخ محلية على سطح المكتب، وبعضها منشور فعليًا عبر GitHub Pages) وحفظ معرفة بها.
2. متابعة استمرارية عمل كل مشروع (هل الموقع شغال أو متوقف).
3. اكتشاف الأخطاء المحتملة والصامتة قبل ما تتفاقم — أخطاء برمجية، أخطاء بيانات، مشاكل واجهة مستخدم.
4. اقتراح خطط إصلاح وخطط تطوير/تحسين للواجهات وللأنظمة.
5. تنفيذ الخطط المتفق عليها — على المشاريع الحالية أو مشاريع مستقبلية — **بعد موافقة محمد الصريحة فقط**، وعرض المقترح والخطة بعربي بسيط بدون مصطلحات تقنية قبل أي تنفيذ.

### آلية المتابعة المستمرة (تم الاتفاق عليها)
- فحص تلقائي دوري (عبر GitHub Actions أو مُشغّلات Google Apps Script المجدولة) يشتغل كل فترة زمنية، يفحص المشاريع، ويسجل النتائج في Google Sheets.
- محمد يفتح المحادثة (هنا أو مع Claude Code) فيقرأ النتائج المسجلة، يحللها، ويعرض خطط الإصلاح.
- التنفيذ الفعلي لأي تعديل لا يبدأ إلا بعد عرض الخطة على محمد بعربي بسيط والحصول على موافقته الصريحة.

### المستخدم
- محمد هو المستخدم الوحيد للنظام — لا صلاحيات متعددة حاليًا.

### الوصول للأكواد
- المشاريع موجودة على GitHub (بعضها public) + نسخ محلية على سطح مكتب اللابتوب. محمد هيعطي Claude Code صلاحية GitHub مباشرة من داخل VS Code.

### معرف قاعدة البيانات (Google Sheet)
```
13P7q7k2VuQTBIzqsbOjfBvpVvekRH61jX_wg6XL_O0s
```
اسم الملف: "المدير الذكي" — الملف موجود وفاضي حاليًا (ورقة واحدة بدون بيانات)، جاهز لإنشاء الجداول فيه تلقائيًا بالكود.

### النموذج التصميمي المُختار (من بين ٤ نماذج تم عرضها)
تم اختيار **"نموذج ٢ — المكتب الهادئ"**: تصميم بطاقات دائرية هادئة، خلفية دافئة فاتحة (قريبة من `#f2efe6`)، لون أساسي أخضر مريح (`--sage: #5c7a63`) ولون ثانوي طيني دافئ (`--clay: #a9714f`)، خط Tajawal، حواف دائرية كبيرة، بطاقات مشاريع بمؤشر دائري (ring) لنسبة السلامة، وقسم "خطة إصلاح مقترحة" بأزرار "موافقة وتنفيذ" و"مناقشة التفاصيل".
> إذا احتاج Claude Code رؤية الكود الفعلي للنموذج، يطلبه من محمد (ملف HTML تم إنشاؤه مسبقًا باسم "نموذج-2-المكتب-الهادئ").

### هيكلية قاعدة البيانات النهائية المتفق عليها

#### ١. الجدول الرئيسي (نظرة عامة على كل المشاريع)
| اسم العمود | الوصف |
|---|---|
| اسم المشروع | — |
| الحالة العامة | سليم / تحذير / متوقف |
| نسبة السلامة | — |
| رابط الموقع | — |
| آخر فحص | تاريخ ووقت |
| عدد الأخطاء المفتوحة | — |
| عدد الخطط بانتظار الموافقة | — |
| اسم الجدول التفصيلي | للربط بجدول تفاصيل هذا المشروع |

#### ٢. جدول تفاصيل كل مشروع (يُنشأ تلقائيًا باسم كل مشروع)
| اسم العمود | الوصف |
|---|---|
| رقم الحالة | — |
| تاريخ ووقت الاكتشاف | — |
| نوع الحالة | ثلاثة أنواع فقط (انظر أدناه) |
| شدة الحالة | ثلاثة مستويات بالألوان (انظر أدناه) |
| الوصف | تفاصيل الحالة |
| الخطة المقترحة | — |
| القرار | بانتظار الرد / موافقة / تعديل مطلوب / رفض |
| ملاحظات تعديلك | تعديل محمد على الخطة إن وُجد |
| حالة التنفيذ | لم يبدأ / جاري التنفيذ جزئيًا / مكتمل بالكامل |
| تاريخ آخر تحديث | — |

**قيم عمود "نوع الحالة" (٣ أنواع بالضبط):**
1. يوقف الموقع بالكامل — الموقع لا يعمل نهائيًا
2. لا يوقف الموقع لكن يسبب أخطاء ظاهرة وتعليق للمستخدمين
3. خطأ صامت — الموقع يعمل ظاهريًا بشكل طبيعي، لكن فيه خلل خفي في البيانات يكبر مع الوقت إذا لم يُكتشف مبكرًا (هذا النوع مهم جدًا لأنه لا يظهر إلا بعد أن يكبر)

**قيم عمود "شدة الحالة" (٣ مستويات بالضبط، مع الألوان):**
| المستوى | المعنى | اللون |
|---|---|---|
| حرجة جدًا | يجب التدخل فورًا | أحمر |
| حرجة متوسطة | تحتاج تدخل قريب لكن غير فوري | برتقالي |
| حرجة غير مستعجلة | لا تستدعي تدخل حالًا | سماوي |

#### ٣. جدول سجل المراحل (مرجع كامل لكل خطة متعددة المراحل)
| اسم العمود | الوصف |
|---|---|
| رقم الحالة | مرتبط بجدول تفاصيل المشروع |
| رقم المرحلة | — |
| وصف المرحلة | — |
| تاريخ التنفيذ | — |
| حالة المرحلة | منتظرة / منفذة |
| ملاحظات | — |

### الخطوة التالية المتفق على البدء فيها
كتابة كود Google Apps Script الذي ينشئ الجداول الثلاثة أعلاه تلقائيًا داخل ملف الشيت المذكور، مع تلوين عمود "شدة الحالة" حسب الألوان المحددة، والصف الأول ثابت وملوّن، والنص في المنتصف، وحجم الخط ١٢ عريض — التزامًا بكل قواعد القسم الأول.

---

## ملاحظة أخيرة لـ Claude Code
قبل كتابة أي كود، راجع القسم الأول والثاني بالكامل، وإذا كان هناك أي تفصيل تقني ناقص (مثل: طريقة فحص GitHub Pages تحديدًا، أو تكرار الفحص كل كام دقيقة/ساعة، أو تفاصيل ربط GitHub Actions) — اسأل محمد مباشرة بعربي بسيط قبل البدء، ولا تفترض أي قيمة افتراضية بنفسك.
